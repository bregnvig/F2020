{
  "name": "cloud",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "nx lint cloud",
    "build": "nx build cloud",
    "serve": "npm run build && firebase emulators:start --config=firebase.json --export-on-exit=./saved-data --import=./saved-data",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --config firebase.json --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": " 16"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
